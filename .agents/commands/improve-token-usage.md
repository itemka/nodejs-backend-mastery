# Improve Token Usage Command

Use this as a runnable prompt to investigate AI-session context cost and
produce an execution-ready token-usage improvement plan.

## Run

1. Read `AGENTS.md` and the rules under `.agents/rules/`.
2. Read and follow [.agents/skills/improve-token-usage/SKILL.md](../skills/improve-token-usage/SKILL.md).
3. Investigation and planning only — do not edit source, config, or
   instruction files while producing the plan.

## User Input

Use the command arguments or latest user message as the optional output path,
scope, or focus note.

## Output

Follow the skill's output format.
