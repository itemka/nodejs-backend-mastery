---
name: debug
description: Use to investigate failing tests, runtime errors, build errors, flaky behavior, or unclear regressions.
---

# Debug

Canonical instructions live in `.agents/skills/debug/SKILL.md`.

When this skill is selected:

1. Read `AGENTS.md`.
2. Read `.agents/README.md` and the rules under `.agents/rules/`.
3. Read `.agents/skills/debug/SKILL.md`.
4. Follow the `.agents` skill as the source of truth.
5. Prefer focused reproduction before broad changes.
