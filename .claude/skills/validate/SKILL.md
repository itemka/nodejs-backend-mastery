---
name: validate
description: Use before finishing work to choose and run relevant tests, typechecks, linting, formatting, builds, or focused verification commands. Triggered by "validate", "run the tests", "check this works", or finalizing a change.
---

# Validate

Canonical instructions live in `.agents/skills/validate/SKILL.md`.

When this skill is selected:

1. Read `AGENTS.md`.
2. Read `.agents/README.md` and the rules under `.agents/rules/`.
3. Read `.agents/skills/validate/SKILL.md`.
4. Follow the `.agents` skill as the source of truth.
5. Prefer the smallest relevant check first, then broader checks when safe.
