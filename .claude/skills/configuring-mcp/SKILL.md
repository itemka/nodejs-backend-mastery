---
name: configuring-mcp
description: MCP configuration and access-boundary review for AI coding tools. Use when planning, reviewing, or documenting MCP servers, external tool access, or MCP config.
argument-hint: '[MCP server or external tool]'
---

# Configuring MCP

Canonical instructions live in `.agents/skills/configuring-mcp/SKILL.md`.

When this skill is selected:

1. Read `AGENTS.md`.
2. Read `.agents/README.md` and the rules under `.agents/rules/`.
3. Read `.agents/skills/configuring-mcp/SKILL.md`.
4. Follow the `.agents` skill as the source of truth.
5. Do not create MCP config or include credentials unless explicitly requested and safely scoped.
