---
name: branch-naming
description: Branch name format and conventions for creating or suggesting git branches. Use when asked to name, create, rename, or choose a git branch.
argument-hint: '[task brief]'
---

# Branch Naming

Canonical instructions live in `.agents/skills/branch-naming/SKILL.md`.

When this skill is selected:

1. Read `AGENTS.md`.
2. Read `.agents/README.md` and the rules under `.agents/rules/`.
3. Read `.agents/skills/branch-naming/SKILL.md`.
4. Follow the `.agents` skill as the source of truth.
