---
name: pr-description
description: Generate concise PR titles, summaries, and logical change breakdowns from a branch diff. Use when asked to write a PR description, prepare PR text, create a pull request, or summarize branch changes for review.
argument-hint: '[base branch or PR context]'
---

# PR Description

Canonical instructions live in `.agents/skills/pr-description/SKILL.md`.

When this skill is selected:

1. Read `AGENTS.md`.
2. Read `.agents/README.md` and the rules under `.agents/rules/`.
3. Read `.agents/skills/pr-description/SKILL.md`.
4. Follow the `.agents` skill as the source of truth.
5. Do not push or open a PR unless the user explicitly asks.
