---
description: Produce a concrete implementation plan using the repo's shared planning workflow.
argument-hint: '[task brief]'
---

Read and execute `.agents/commands/plan.md` as the canonical command body.
This command is an explicit plan request, so create or update the plan file
described there unless the user explicitly says not to create a file.

Also follow:

- `AGENTS.md`
- `.agents/README.md`
- `.agents/rules/project.md`
- `.agents/rules/repo-map.md`
- `.agents/rules/change-discipline.md`

Use the user-provided arguments as the planning brief.
