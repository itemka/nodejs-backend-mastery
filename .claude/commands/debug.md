---
description: Investigate a failing test, runtime error, build error, or regression using the repo's shared debug workflow.
argument-hint: '[failing command or symptom]'
---

Read and execute `.agents/commands/debug.md` as the canonical command body.

Use the user-provided arguments as the debugging brief.
