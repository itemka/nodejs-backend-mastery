---
description: Run scoped validation for changed files using the repo's shared validation workflow.
argument-hint: '[optional scope or validation context]'
---

Read and execute `.agents/commands/validate-changed.md` as the canonical command body.

Use the user-provided arguments as validation context.
